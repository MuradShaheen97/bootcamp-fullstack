-- Run this file once in MySQL Workbench before starting the project.

CREATE DATABASE IF NOT EXISTS project_manager_db
CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

-- You do not need to create tables manually.
-- Hibernate creates and updates the tables from the Java model classes.
