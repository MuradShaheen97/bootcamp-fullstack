This assignment helped me understand how functions actually behave in Python.

At first, I thought print and return are almost the same, but now I clearly see the difference.

I also learned that once a function hits return, it stops immediately, and anything after it will not run.

Another important thing was understanding scope — variables inside functions are not the same as outside.

Some questions were tricky, especially when functions didn’t return anything and caused errors.

Overall, this assignment made me slow down and really think about how the code is executed step by step.