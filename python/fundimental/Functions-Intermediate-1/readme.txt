
## randInt Function

A simple function that returns a random number without using `randint()`.

It uses `random.random()` to generate a number within a range:

* No input → between 0 and 100
* One number → from 0 to that number
* Two numbers → between min and max

The idea is to build the logic manually and understand how random ranges work.
